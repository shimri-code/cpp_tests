#include <iostream>
#include <vector>

// #include "tools/stack.h"

int main(int argc, char **argv)
{
    // testing::InitGoogleTest(&argc, argv);
    // return RUN_ALL_TESTS();

    // std::cout << "Main " << std::endl;
    // Stack<int> myStack(10);
    // Stack<int> myStack2 = Stack<int>(10);

    // myStack.push(1);
    // myStack.push(2);
    // myStack.push(3);
    // myStack.push(4);

    // std::cout << myStack << std::endl;

    // while (!myStack.isEmpty())
    // {
    //     int n = myStack.pop();
    //     std::cout << n << std::endl;
    // }

    int x;
    std::cin >> x;

    return 0;
}